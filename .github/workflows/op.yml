name: openwrt-official

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      OP_IP:
        description: '静态IP地址'
        required: false
        default: '192.168.2.2'
        type: string

      ENABLE_DHCP:
        description: '是否开启DHCP'
        required: false
        default: 'true'
        type: boolean

      OP_rootfs:
        description: '镜像根分区大小 (MB)'
        required: false
        default: '600'
        type: string

      OP_author:
        description: '定制作者'
        required: false
        default: 'djkyc'
        type: string

env:
  DIY_P1_SH: sh/op.sh

jobs:
  build_firmware:
    name: 官方 OpenWrt 编译 - 玩客云(ws1608)
    runs-on: ubuntu-22.04

    steps:
    - name: 检查分支
      uses: actions/checkout@v4

    - name: 释放磁盘空间
      run: |
        sudo swapoff -a || true
        sudo rm -f /swapfile || true
        sudo docker image prune -a -f
        sudo apt-get -y purge firefox clang* ghc* google* llvm* mono* mysql* php* || true
        sudo apt-get -y autoremove --purge
        sudo apt-get clean
        df -hT

    - name: 设置编译环境
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential flex bison cmake g++ gawk gcc-multilib g++-multilib \
          gettext git libfuse-dev libncurses5-dev libssl-dev python3 python3-pip rsync unzip zlib1g-dev \
          file wget subversion patch upx-ucl autoconf automake curl asciidoc binutils bzip2 \
          texinfo libreadline-dev libglib2.0-dev xmlto libelf-dev libtool autopoint gperf ccache \
          swig coreutils haveged scons libpython3-dev rename qemu-utils
        sudo apt-get clean
        sudo timedatectl set-timezone 'Asia/Shanghai'
        git config --global user.name 'GitHub Actions'
        git config --global user.email 'noreply@github.com'
        mkdir -p $GITHUB_WORKSPACE/workdir
        sudo chown -R $USER:$GROUPS $GITHUB_WORKSPACE/workdir

    - name: 设置环境变量
      run: |
        TAG="$(date +'%Y%m%d')-$(openssl rand -hex 3)"
        echo "tag=$TAG" >> $GITHUB_ENV
        echo "REPO_URL=https://github.com/openwrt/openwrt" >> $GITHUB_ENV
        echo "REPO_BRANCH=openwrt-24.10" >> $GITHUB_ENV
        echo "OP_IP=${{ github.event.inputs.OP_IP }}" >> $GITHUB_ENV
        echo "ENABLE_DHCP=${{ github.event.inputs.ENABLE_DHCP }}" >> $GITHUB_ENV
        echo "OP_rootfs=${{ github.event.inputs.OP_rootfs }}" >> $GITHUB_ENV
        echo "OP_author=${{ github.event.inputs.OP_author }}" >> $GITHUB_ENV

    - name: 克隆官方源码
      working-directory: ${{ github.workspace }}/workdir
      run: |
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf $GITHUB_WORKSPACE/workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: 获取最新标签（官方 release）
      working-directory: ./openwrt
      run: |
        _release_tag=$(git tag --sort=taggerdate --list 'v24.*' | tail -1)
        git checkout $_release_tag
        echo "release_tag=$_release_tag" >> $GITHUB_ENV

    - name: 恢复编译缓存
      uses: actions/cache@v4
      with:
        path: openwrt/staging_dir
        key: ${{ env.REPO_BRANCH }}-toolchain-cache
        restore-keys: |
          ${{ env.REPO_BRANCH }}-toolchain-cache

    - name: 下载 feeds
      working-directory: ./openwrt
      run: |
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: 加载自定义脚本
      run: |
        chmod +x $DIY_P1_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P1_SH

    - name: 添加配置文件
      run: |
        cat <<EOF > openwrt/.config
        CONFIG_TARGET_amlogic=y
        CONFIG_TARGET_amlogic_meson8b=y
        CONFIG_TARGET_amlogic_meson8b_DEVICE_thunder-onecloud=y
        CONFIG_TARGET_ROOTFS_PARTSIZE=${{ github.event.inputs.OP_rootfs }}
        CONFIG_TARGET_KERNEL_PARTSIZE=32
        CONFIG_KERNEL_BUILD_USER="${{ github.event.inputs.OP_author }}"
        CONFIG_KERNEL_BUILD_DOMAIN="github.com"
        CONFIG_DEVEL=y
        CONFIG_CCACHE=y
        CONFIG_PACKAGE_luci=y
        CONFIG_LUCI_LANG_zh_Hans=y
        EOF

        if [[ "${{ github.event.inputs.ENABLE_DHCP }}" == "false" ]]; then
          sed -i "s/192.168.1.1/${{ github.event.inputs.OP_IP }}/" openwrt/package/base-files/files/bin/config_generate
        fi

    - name: 生成配置并下载依赖
      working-directory: ./openwrt
      run: |
        make defconfig
        make download -j$(nproc)
        find dl -size -1024c -exec rm -f {} \;

    - name: 编译源码
      working-directory: ./openwrt
      run: |
        echo "开始编译 $(nproc) 线程"
        make -j$(nproc) || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: 整理固件文件
      id: organize
      run: |
        cd openwrt/bin/targets/*/*
        xz -9 --threads=0 *.img || true
        rm -rf packages *.buildinfo profiles.json *.manifest *.sha sha256sums
        mv *.xz openwrt-official-${{ env.release_tag }}-onecloud.img.xz
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV

    - name: 上传固件到Releases
      uses: softprops/action-gh-release@v2.1.0
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ env.release_tag }}-${{ env.tag }}
        files: ${{ env.FIRMWARE }}/*
        body: |
          ==================== 固件信息 =======================
          📦 源码：${{ env.REPO_URL }}
          🌿 分支：${{ env.release_tag }}
          💻 设备：玩客云 (ws1608)
          🧩 内核：官方 6.6
          🌐 静态IP：${{ github.event.inputs.OP_IP }}
          🔧 DHCP：${{ github.event.inputs.ENABLE_DHCP }}
          👤 作者：${{ github.event.inputs.OP_author }}
          🧱 根分区：${{ github.event.inputs.OP_rootfs }}MB
          🧰 默认密码：无

    - name: 清理旧 Releases
      uses: dev-drprasad/delete-older-releases@v0.3.4
      with:
        keep_latest: 1
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
